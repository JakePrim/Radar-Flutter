<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.edu.dao.MenuMapper">
    <resultMap id="menuMap" type="Menu">
        <id column="id" property="id"></id>
        <result column="href" property="href"></result>
        <result column="icon" property="icon"></result>
        <result column="name" property="name"></result>
        <result column="parent_id" property="parentId"></result>
        <result column="description" property="description"></result>
        <result column="order_num" property="orderNum"></result>
        <result column="shown" property="shown"></result>
        <result column="created_time" property="createdTime"></result>
        <result column="updated_time" property="updatedTime"></result>
        <result column="created_by" property="createdBy"></result>
        <result column="updated_by" property="updatedBy"></result>

        <collection property="subMenuList" ofType="Menu"
                    select="findAllMenu" column="id">

        </collection>
    </resultMap>
    <insert id="saveMenu" parameterType="Menu">
        insert into menu
        values (null, #{parentId}, #{href}, #{icon}, #{name}, #{description}, #{orderNum}, #{shown}, #{level},
                #{createdTime}, #{updatedTime}, #{createdBy}, #{updatedBy})
    </insert>
    <update id="updateMenu" parameterType="Menu">
        update menu
        <trim prefix="set" suffixOverrides=",">
            <if test="parentId != null and parentId != ''">
                parent_id = #{parentId},
            </if>
            <if test="href != null and href != ''">
                href = #{href},
            </if>
            <if test="icon != null and icon != ''">
                icon = #{icon},
            </if>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="description != null and description != ''">
                description = #{description},
            </if>
            <if test="orderNum != null and orderNum != ''">
                order_num = #{orderNum},
            </if>
            <if test="shown != null and shown != ''">
                shown = #{shown},
            </if>
            <if test="level != null and level != ''">
                level = #{level},
            </if>
            <if test="updatedTime != null">
                updated_time = #{updatedTime},
            </if>
            <if test="updatedBy != null and updatedBy != ''">
                updated_by = #{updatedBy},
            </if>
        </trim>
        <where>
            <if test="id != null and id != 0">
                id = #{id}
            </if>
        </where>
    </update>

    <!-- 查询所有父子关系的菜单 -->
    <select id="findSubMenuListByPid" resultMap="menuMap" parameterType="int">
        select *
        from menu
        where parent_id = #{id}
    </select>
    <select id="findMenuIdByRoleId" resultType="java.lang.Integer" parameterType="int">
        select menu_id
        from role_menu_relation
        where role_id = #{roleId}
    </select>
    <select id="findAllMenu" resultType="Menu">
        select *
        from menu
    </select>
    <select id="findMenuById" resultType="com.edu.pojo.Menu" parameterType="int">
        select *
        from menu
        where id = #{id}
    </select>
</mapper>